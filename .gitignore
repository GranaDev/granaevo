.vercel
# Todos os arquivos .env (com e sem sufixo .local, qualquer ambiente)
.env
.env.*
!.env.example
node_modules/
dist/
# Metadados do Supabase CLI (contêm project_ref e org_id)
supabase/.temp/
# Relatórios de segurança — não commitar (expõem vulnerabilidades internas)
SECURITY_AUDIT_REPORT.md
security-audit/

# Artefato de análise de CSS não usado (temporário, não versionar)
css-unused-candidates.txt
